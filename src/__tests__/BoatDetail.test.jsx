import React from 'react';
import { describe, it, expect } from 'vitest';
import { render } from '@testing-library/react';

describe('BoatDetail Component', () => {
  it('should pass basic test', () => {
    expect(1 + 1).toBe(2);
  });
});